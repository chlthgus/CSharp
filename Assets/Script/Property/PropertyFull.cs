using UnityEngine;

public class PropertyFull : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        /*
       
        Person person = new Person();

        person.SetName "홍길동";
        Debug.Log(dev.SetName);
      */
        } 
}
