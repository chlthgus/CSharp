using Unity.VisualScripting;
using UnityEngine;

namespace Property
{
    public class ProprertyPrinate : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            //Page.page = new Page();

        }
    }
}