using Indexer;
using UnityEngine;

public class IndexerD : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Catalog ca = new Catalog();
        //Debug.Log(ca[0]); //인스턴스이름[인덱스] 형태로 사용
    }
     
}
